# Shuffle SOC Workflow demo saved searches
[High Severity Alert]
search = index=main severity=high | stats count by rule_name, src_ip | where count > 1
alert.track = 1
alert.severity = 3
cron_schedule = */5 * * * *
dispatch.earliest_time = -5m
dispatch.latest_time = now
