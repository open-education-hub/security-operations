
services:
  win-security:
    build: .
    container_name: win-security-demo
    volumes:
      - ./scripts:/scripts
      - ./data:/data
      - ./reports:/reports
    stdin_open: true
    tty: true
